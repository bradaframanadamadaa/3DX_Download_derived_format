namespace DerivedOutputDownloader3DX.Logging;

/// <summary>Préfixes de journal cohérents avec les outils Reconciliation (FedSearch, 3DPassport, dsdo).</summary>
public static class LogPrefixes
{
    public const string FedSearch = "[FedSearch]";
    public const string Passport = "[3DPassport]";
    public const string ThreeDSpace = "[3DSpace]";
    public const string DerivedOutput = "[dsdo]";
}
